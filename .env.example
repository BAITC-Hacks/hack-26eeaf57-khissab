# Compose loads these defaults, then optional .env overrides (Compose 2.24+).
# Backend
APP_ENV=local
# One-click jury access to synthetic local data, including HR uploads.
# Requires APP_ENV=local. Set false for private account-code sign-in.
AUTH_DEMO_MODE=true
DATA_DIR=/app/data
# Container /app/storage is mounted from the gitignored repo /storage/ directory.
# First startup creates and seeds this SQLite file; restarts preserve progress.
DATABASE_URL=sqlite:////app/storage/career_quest.sqlite3
# Local run from the repo root: DATA_DIR=./data
# Local run from the repo root: DATABASE_URL=sqlite:///./storage/career_quest.sqlite3
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# P2 scoring is offline; fixed weights live in backend/engine.py, not environment variables.
# Optional explanation provider: openai, local, or template (strict offline mode).
# No key is needed to start: missing keys always use deterministic templates.
LLM_PROVIDER=openai
OPENAI_API_KEY=
LLM_MODEL=gpt-4o-mini
# Only used with LLM_PROVIDER=local; OpenAI always uses https://api.openai.com/v1.
LLM_BASE_URL=
LLM_API_KEY=
# Total request deadline; capped at 8 seconds. Up to 3 explanations run concurrently.
LLM_TIMEOUT_SECONDS=8

# Frontend
VITE_API_URL=/api
# Docker Compose points the Vite dev proxy at the backend service.
# Local Vite defaults to http://127.0.0.1:8000 when this is unset.
VITE_API_PROXY_TARGET=http://backend:8000
